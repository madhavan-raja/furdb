version: "3"
services:
  api:
    container_name: api
    image: madhavanraja/furdb
    environment:
      WORKDIR: /furdb
      PORT: 8080
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    develop:
      watch:
        - action: rebuild
          path: .
